import "./Hero.css";

export default function Hero() {
  return (
    <section className="hero">
      <div className="hero-overlay">
        <h1>Bridge the Gap, Stop Food Waste</h1>
        <p>Connecting food providers with those in need.</p>
        <button>Get Started</button>
      </div>
    </section>
  );
}
